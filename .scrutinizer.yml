build:
  environment:
    php: '8.0'

  dependencies:
    before:
      - composer install --no-interaction --prefer-dist --ignore-platform-req=php

  tests:
    override:
      - php-scrutinizer-run

checks:
  php: true